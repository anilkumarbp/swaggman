'use strict';

module.exports = (swaggerJSON, path, verb) => {
    // TODO: Need to complete this
    let vars = [];
    let varSchema = {
        id: '',
        value: null,
        type: '',
        name: ''
    };
    vars.push(varSchema);
    return vars;
};
